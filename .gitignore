# [VPT-VEDA] Build Outputs (Strict Rule)
build/
build_host/
*.tar.gz
*.so
*.a
*.o

# 정적분석 산출물 (경로 종속 / 런타임 생성 — 절대 커밋 금지)
compile_commands.json
tools/misra.json

# STM32 펌웨어 빌드 산출물 (보통 build/ 아래지만 안전차원)
*.elf
*.map
*.su
*.hex
*.bin

# macOS
.DS_Store

# STM32CubeMX & CLion / clangd IDE Tools
.idea/
.cmake/
.cache/
CMakeLists.txt.user
Documents/
*.mxproject

# Yocto / Linux Kernel Development Artifacts
*.ko
*.mod.c
*.mod
modules.order
Module.symvers
.tmp_versions/
